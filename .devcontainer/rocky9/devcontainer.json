{
    "name": "Aurora LTFS Development (Rocky Linux 9)",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "../.."
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "llvm-vs-code-extensions.vscode-clangd",
                "ms-vscode.makefile-tools"
            ]
        }
    },
    "mounts": [
        "source=${localEnv:HOME}/.claude,target=/home/vscode/.claude,type=bind"
    ],
    "remoteUser": "vscode"
}
